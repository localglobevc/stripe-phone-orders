import React from 'react';
import styled from 'styled-components';
import {Button, Form} from 'semantic-ui-react';

export const PrimaryButton = styled(Button)`
  background-color: #4BCC93;
  color: white;
`;
